import { FC } from 'react';
interface CreateNewStatusModalProps {
    isModalOpen?: boolean;
    handleCloseModal?: () => void;
}
declare const CreateNewStatusModal: FC<CreateNewStatusModalProps>;
export default CreateNewStatusModal;
