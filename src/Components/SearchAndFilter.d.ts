import { FC } from 'react';
interface SearchAndFilterProps {
    handleOpenCreateNewStatusModal?: () => void;
}
declare const SearchAndFilter: FC<SearchAndFilterProps>;
export default SearchAndFilter;
